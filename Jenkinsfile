pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'building image...'
            }
        }

        stage('Push Docker HUB') {
            steps {
                echo 'push docker image to docker hub...'
            }
        }
    }
}
